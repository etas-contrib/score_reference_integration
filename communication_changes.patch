diff --git a/score/mw/com/requirements/assumed_system_requirements/BUILD b/score/mw/com/requirements/assumed_system_requirements/BUILD
index 0142ce5..8b0e93e 100644
--- a/score/mw/com/requirements/assumed_system_requirements/BUILD
+++ b/score/mw/com/requirements/assumed_system_requirements/BUILD
@@ -19,7 +19,7 @@ trlc_requirements(
         "assumed_system_requirements.trlc",
     ],
     spec = [
-        "@//third_party/traceability/config/trlc/score_model:requirement_metamodel",
+        "//third_party/traceability/config/trlc/score_model:requirement_metamodel",
     ],
     visibility = ["//visibility:public"],
 )
diff --git a/score/mw/com/requirements/feature_requirements/BUILD b/score/mw/com/requirements/feature_requirements/BUILD
index 66a12ed..459a967 100644
--- a/score/mw/com/requirements/feature_requirements/BUILD
+++ b/score/mw/com/requirements/feature_requirements/BUILD
@@ -20,7 +20,7 @@ trlc_requirements(
         "//score/mw/com/requirements/assumed_system_requirements",
     ],
     spec = [
-        "@//third_party/traceability/config/trlc/score_model:requirement_metamodel",
+        "//third_party/traceability/config/trlc/score_model:requirement_metamodel",
     ],
     visibility = ["//visibility:public"],
 )
diff --git a/score/mw/com/runtime_test.cpp b/score/mw/com/runtime_test.cpp
index 79980b7..fc1b1bd 100644
--- a/score/mw/com/runtime_test.cpp
+++ b/score/mw/com/runtime_test.cpp
@@ -82,10 +82,24 @@ class RuntimeTestExternalJsonFixture : public ::testing::Test
             file.close();
             return default_path;
         }
-        else
-        {
-            return "external/safe_posix_platform/" + default_path;
+
+        // Try multiple possible external repository names
+        std::vector<std::string> external_paths = {
+            "external/safe_posix_platform/" + default_path,
+            "external/communication+/" + default_path,
+            "external/score_communication+/" + default_path
+        };
+
+        for (const auto& path : external_paths) {
+            std::ifstream ext_file(path);
+            if (ext_file.is_open()) {
+                ext_file.close();
+                return path;
+            }
         }
+
+        // Return the original fallback if nothing works
+        throw std::runtime_error("Could not find configuration file: " + file_name);
     }
 };
 
diff --git a/third_party/traceability/bazel/safety_analysis.bzl b/third_party/traceability/bazel/safety_analysis.bzl
index 24e30b8..078b5f2 100644
--- a/third_party/traceability/bazel/safety_analysis.bzl
+++ b/third_party/traceability/bazel/safety_analysis.bzl
@@ -16,13 +16,13 @@ def safety_analysis(
 
     lobster_trlc(
         name = FMEA_FAILUREMODES,
-        config = "@//third_party/traceability/config/lobster:safety_analysis_conf",
+        config = "//third_party/traceability/config/lobster:safety_analysis_conf",
         requirements = failuremodes,
     )
 
     lobster_trlc(
         name = FMEA_CONTROLMEASURES,
-        config = "@//third_party/traceability/config/lobster:safety_analysis_conf",
+        config = "//third_party/traceability/config/lobster:safety_analysis_conf",
         requirements = controlmeasures,
     )
 
@@ -50,7 +50,7 @@ def safety_analysis(
     LOBSTER_CONFIG = "{}_lobster_config".format(name)
     expand_template(
         name = LOBSTER_CONFIG,
-        template = "@//third_party/traceability/config/lobster:traceability_safety_analysis_conf",
+        template = "//third_party/traceability/config/lobster:traceability_safety_analysis_conf",
         out = "{}_traceability_config".format(name),
         substitutions = {
             "fmea_control_measures": FMEA_CONTROLMEASURES,
